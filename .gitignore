# build output
helper/build/
*.o
node_modules/
app/dist/
# recordings & spike artifacts
sessions/
scratch/out*/
scratch/*.app/
scratch/cammotion
scratch/gen
scratch/encbench
scratch/smoke
.DS_Store

# fixture generator binary
fixtures/gen-display

# signing-probe bundle (regenerable)
tools/signing-probe/*.app/
tools/test-host/*.app/

# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/

# Session-local git worktree checkouts (EnterWorktree/ExitWorktree "keep") —
# a full nested checkout with its own node_modules and .git, never a
# deliverable. Found untracked at ~471MB after a "keep" exit with no
# .gitignore entry to catch it (STC-444 slice 4, 2026-09-24).
.claude/worktrees/
